# TrueSIP reads GROQ_API_KEY for the "explain-why" layer (OPTIONAL).
# Without it, the app runs in demo mode with deterministic template explanations,
# so any visitor still sees a fully working product.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=
