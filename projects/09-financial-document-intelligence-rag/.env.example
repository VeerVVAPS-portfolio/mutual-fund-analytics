# Copy this file to `.env` and fill in your key.
# The app runs WITHOUT a key in demo mode (real retrieval, pre-written answers).
# A key unlocks live LLM synthesis of the retrieved excerpts.

# Groq API key — free at https://console.groq.com
GROQ_API_KEY=
